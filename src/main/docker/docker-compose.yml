version: "2"
services:
  modelerW2V:
    container_name: modelerW2V
    image: librairy/modeler-w2v:latest
    #restart: always
    volumes:
         - ./librairy:/librairy
    external_links:
         - column-db
         - document-db
         - graph-db
         - event-bus
    networks:
         - "db"
         - "events"
networks:
  db:
    external:
      name: base_db
  events:
    external:
      name: base_events